# MyMenuAgent

A mobile-first PWA, doubling as the restaurant's own public website, that lets a single owner photograph their handwritten daily specials board, generate an on-brand styled image of it via AI, review/approve it, and publish it to the live site instantly with no redeploy.

## Source of truth

The architecture and plan in `docs/` is the **source of truth for all sessions**. It was designed and confirmed with the owner on 2026-07-06. Read the relevant doc before designing or implementing anything in its area, and follow it. If an implementation need genuinely conflicts with the docs, surface the conflict and propose a doc update — do not silently diverge, and do not re-litigate settled decisions (especially the stack).

| Doc | Contents |
|---|---|
| [docs/01-tech-stack.md](docs/01-tech-stack.md) | Confirmed stack and rationale |
| [docs/02-architecture.md](docs/02-architecture.md) | System diagram, no-redeploy publish path, scheduling mechanism |
| [docs/03-data-model.md](docs/03-data-model.md) | Full schema, multi-tenant seam, design notes |
| [docs/04-api-surface.md](docs/04-api-surface.md) | Public + authenticated endpoints |
| [docs/05-provider-abstraction.md](docs/05-provider-abstraction.md) | AI provider layer: interfaces, registry, metering |
| [docs/06-pwa-modules.md](docs/06-pwa-modules.md) | Admin module breakdown + public marketing site pages |
| [docs/07-roadmap.md](docs/07-roadmap.md) | Phased build plan (Phase 0 → 4) and deferred items |
| [docs/08-risks.md](docs/08-risks.md) | Key risks and design mitigations |
| [docs/09-compliance.md](docs/09-compliance.md) | Florida legal compliance posture, breach/retention procedures (not legal advice) |

## Confirmed stack (do not re-litigate)

- **Next.js 15 (App Router), TypeScript, Tailwind** — PWA + API routes + the restaurant's public marketing site, deployed on **Netlify** (superseded Vercel on 2026-07-14, owner's explicit call — see "Architecture pivot" below)
- **Supabase** — Postgres (RLS on `restaurant_id`), Auth, Storage (private buckets). Provider API keys are encrypted **app-side with AES-256-GCM** (`src/lib/providers/crypto.ts`), **not** Supabase Vault — this line said "Vault" until 2026-07-16; Vault was in the original plan and never used.
- **satori + resvg** for template PNG exports; **Netlify Scheduled Functions** for scheduled publishing

## Non-negotiable design rules

1. **Nothing restaurant-specific is ever hardcoded** — all identity, branding, and menu defaults come from the `restaurants` profile row. The public site and the Daily Specials image pipeline resolve branding from the profile at render/generation time.
2. **Every table carries `restaurant_id`** — this is the multi-tenant seam. Adding a restaurant must mean adding a row, never changing code.
3. **Publishing is a pointer flip** — immutable `published_snapshots` + `restaurants.live_snapshot_id`. Never introduce a publish path that requires a build or deploy.
4. **Provider API keys never reach the client** — encrypted at rest, decrypted only inside server handlers, never logged or echoed (display last-4 only). All AI calls go through the provider abstraction (capability interfaces + adapters), never direct vendor SDK calls from feature code.
5. **All surfaces read the same public API** (`GET /api/public/{slug}/menu` for Daily Specials, `GET /api/public/{slug}/main-menu` for the permanent menu): the site's own `/menu` page and homepage today, any future consumer (e.g. social publishing) later.
6. **Prices are integer cents** (`price_cents`) with a free-text `price_note` escape hatch.

## Explicitly deferred (design for, do not build)

- **Social-media posting** — future `publish_targets` type; see extension notes in docs/07.
- **Multi-tenant onboarding/billing** — schema is already multi-tenant-ready; only UI/billing work remains later.

## Project scaffold notes

- Scaffolded with `create-next-app` (App Router, TypeScript, Tailwind, `src/` dir, `@/*` import alias, npm). Resolved to Next.js 16.x (latest at scaffold time, docs originally said 15 — see docs/01's "Implementation notes" section).
- **Still not a git repository, and stays that way — owner drives git/GitHub exclusively via GitHub Desktop (2026-07-15, owner's explicit, repeated call):** a local `git init` + commit was briefly tried here to help prep a first push, but the owner explicitly said not to touch git/GitHub at all ("I told you do not touch anything regarding GitHub... I will push everything") and had the `.git` folder removed. **Claude Code must never run `git` or `gh` commands in this project** — no `init`, `add`, `commit`, `remote`, `push`, nothing — only edit plain files; the owner stages/commits/pushes everything themselves through GitHub Desktop. Netlify is wired to continuous deployment from a GitHub repo the owner manages entirely outside this tool.
- `AGENTS.md` at the repo root is a generic Next.js agent note generated by the scaffold; this file (`CLAUDE.md`) is the authoritative one for Claude Code sessions.
- Route guard/session-refresh logic is `src/proxy.ts`, not `middleware.ts` — Next.js 16 renamed the convention.
- `npm run dev` / `npm run build` pass `--webpack` explicitly — Next 16 defaults to Turbopack, which the PWA tooling (`serwist`) doesn't yet support. Don't drop the flag.
- Phase 0 is complete: scaffold, full Supabase schema + RLS (`supabase/migrations/`), email + password auth (see the auth note below — originally magic-link), restaurant profile API, Settings screen, PWA shell.
- Phase 1 is complete: Gemini vision adapter behind the provider registry (`src/lib/providers/`), encrypted key vault + task config (Settings → AI Providers), Storage upload flow, capture → parse → review → publish loop, one theme ("classic"), and the public API + hosted page + embeddable widget all reading the same live snapshot. See [README.md](README.md)'s "Try the Phase 1 golden path" for how to exercise it end to end, and its "Known gaps" for placeholder icons / manual owner bootstrap.
- Phase 2 is complete: OpenAI + xAI adapters (`src/lib/providers/openai-compatible.ts`, shared since xAI's API is OpenAI-wire-compatible), comparison mode (`/menus/new/compare`, `POST /api/menus/compare`), three more themes + per-menu style overrides on the Design/Publish screen, scheduling (`scheduleMenuPublish` + `/api/cron/promote-schedules`), history + one-tap re-publish (`/history`), and the item library (learn on save, fuzzy-suggest on Review, manage at `/library`). See README's "Try the Phase 2 features".
- `provider_credentials.encrypted_key` is stored as base64 **text**, not `bytea` as docs/03 literally says — PostgREST serializes bytea ambiguously (hex-escaped string), so base64 text avoids a fragile round-trip. Documented in the migration and docs/01.
- Bulk menu-content saves (parse output and the Review screen's save) go through a single Postgres function, `replace_menu_content()` (`supabase/migrations/..._replace_menu_content.sql`), so the delete+reinsert of sections/items is one transaction — supabase-js has no multi-statement client transaction. Item-library learning and fuzzy suggest are the same pattern: `learn_items_from_menu()` and `suggest_item_library()` (`supabase/migrations/..._item_library_functions.sql`), the latter needed because plain `ilike` wouldn't use the `pg_trgm` GIN index or rank by similarity.
- A newer `eslint-plugin-react-hooks` rule (`react-hooks/static-components`) flags "component created during render" for registry-style theme lookups. Fixed by exposing a single stable `<ThemeRenderer themeKey payload>` component (`src/lib/themes/registry.tsx`) rather than returning a component reference from a lookup function — don't reintroduce the `getThemeRenderer()`-returns-a-component pattern.
- Another `react-hooks` rule, `rules-of-hooks`, false-positives on any function named `use<Something>` even if it's a plain event handler, not a hook — don't name callbacks that way (renamed one `useResult` → `pickResult` in the comparison-mode UI).
- `/api/cron/promote-schedules` must stay a **GET** handler — the Netlify Scheduled Function that invokes it (`netlify/functions/promote-schedules.mts`) sends a GET with `Authorization: Bearer $CRON_SECRET`.
- The "chalkboard" theme (`src/lib/themes/chalkboard.tsx`) intentionally renders on a fixed dark backdrop rather than `restaurant.brand.colors.background` — a chalkboard reads as a chalkboard regardless of the brand's light/dark scheme. Brand fonts and accent color still flow through normally.
- Next up per docs/07: Phase 3 (satori template exports, provider usage dashboard, offline capture queue) — the AI style-reference export and hosted-page items originally planned for this phase are both superseded, see the pipeline/widget-removal notes above.
- If a Supabase project's migrations are run by hand through the SQL editor (no Supabase CLI/access token available), the project may be missing its usual default privilege grants on the `public` schema — every table comes back "permission denied" even for `service_role` (which normally bypasses RLS but still needs a base GRANT). RLS policies are necessary but not sufficient. Fixed permanently by `supabase/migrations/20260706000013_grant_schema_privileges.sql`; if a fresh project hits this again, re-running that file's GRANT statements is safe (idempotent).
- `getCurrentRestaurant()` (`src/lib/auth/current-restaurant.ts`) and `getRestaurantIdOrThrow()` (`src/lib/auth/restaurant-id.ts`) now `console.error` on a real Supabase query error instead of silently treating it the same as "no restaurant linked yet" — that silent-swallow is what made the above permissions gap look like a missing bootstrap step instead of a DB error. Keep surfacing errors from these two lookups if you touch them.
- **Architecture pivot (2026-07-14, owner's explicit call):** this app is now the restaurant's entire public website, not just a menu tool paired with a widget on a separate site. The old `(app)` route group moved to a real `src/app/admin/` segment (PWA scoped there); a new `src/app/(marketing)/` route group owns the public pages (Home, Menu, About, Visit, Gallery, Catering, Order) at the root, fully profile-driven via `src/lib/site/restaurant.ts`. `src/proxy.ts`'s matcher is inverted accordingly — only `/admin`, auth, and authenticated APIs run the session middleware; public pages never pay that cost.
- **[SUPERSEDED — kept only as the reason not to retry it. See the extract-and-render note directly below for how Daily Specials actually works today; this describes a pipeline that no longer exists.]** **Daily Specials image pipeline (2026-07-15, owner's explicit call):** "the specials menu is always going to be an image" — Daily Specials replaced OCR-parse-to-structured-text with AI *image generation*: capture a photo of the handwritten board → OpenAI `gpt-image-1` (`imageGen` capability, docs/05) generates a styled image of it (owner picks a style preset, can regenerate) → owner approves on the merged Review & Publish screen → publish. `menus.generated_image_path` (public `site-media` bucket) drives every consumer via `payload.menu.imageUrl` in the snapshot; the old structured `menu_sections`/`menu_items`/`ParsedMenu` path is legacy, kept only so old pre-pivot snapshots in History still render (docs/03).
- **Daily Specials extract-and-render refactor (2026-07-16):** the gpt-image-1 "hand-letter the whole menu" approach above produced soft/garbled text with real price errors (docs/08) — image models can't reliably re-letter dense menus. Replaced with **AI extracts, app renders**: capture photo → `POST /api/menus/parse-special` → vision model (OpenAI `gpt-4o`, `extractJson` capability on the vision provider) returns structured `DailySpecialMenu` JSON via strict Structured Outputs, Zod-validated (`src/lib/menu/special-menu-schema.ts`), stored in the new `menus.special_data` jsonb → owner **edits the extracted fields** on the Review screen (low-confidence entrees + `uncertainItems` highlighted) → `POST /api/menus/[id]/render-special` renders a **deterministic SVG** (`src/lib/menu/render-special-menu-svg.ts`, a pure function; every price/name is a real `<text>` node, crisp and exact) with a code **theme** (`special-menu-themes.ts`, replacing prompt-fragment styles) → uploads `.svg` to `site-media` as `generated_image_path` → publish (unchanged snapshot flow; SVG works in the existing `<img src={imageUrl}>` path). The renderer is pure so the Review screen's live client-side preview and the server-rendered published artifact are byte-identical. Legacy gpt-image-1 path (`generate-image-service.ts`, `image_gen` default) is kept, not deleted, so old PNG snapshots still render. The prompt-based `image-styles.ts` / `custom_image_styles` from 2026-07-16-earlier are now orphaned for the primary path (not deleted; harmless).
- **Widget + hosted page fully removed (2026-07-15, owner's explicit call: "Delete the feature entirely"):** once this app became the restaurant's own site (the pivot above), the embeddable `widget.js`, `/embed/*`, and the standalone `/m/{slug}` hosted page had no remaining purpose and were deleted outright — not just de-documented. Along with them went Compare mode (`/menus/new/compare`, `ComparisonRunner`), the OCR-parse route/service, the old structured-review editor, and everything that cascaded from removing those (list/create menus, the design route, item-library suggest, the themes list endpoint — all zero-caller after the UI that used them was gone). Verified via systematic `fetch()`-caller grep before each deletion and a clean `tsc`/`eslint` pass after. `docs/02`'s "one public API" rule now names the site's own pages and History's live preview as the real consumers, not a widget.
- **Main Menu vs. Daily Specials:** `menus`/`menu_sections`/`menu_items` (the AI capture→parse→review→publish pipeline) is now scoped to **Daily Specials only**. A separate, hand-typed **Main Menu** (`main_menu_sections`/`main_menu_items`, edited at `/admin/main-menu`) is always-live with no publish step — same "direct edit" model as the profile itself. `main_menu_sections` also carries `description` (shared instructional text like "Served with soup or side salad...") and `category` (`breakfast`/`lunch_dinner`/`beverages`, drives the public `/menu` page's jump navigation). See docs/03's "Main Menu vs. Daily Specials" design note.
- **Site Photos** (`/admin/site`): staff-uploadable hero + gallery images, stored in a **public** `site-media` Storage bucket (deliberately separate from the private `assets` bucket menu photos use — anonymous visitors need to load these with no signed-URL churn). See `src/lib/site-media/` and `src/lib/site/media.ts`.
- **On-demand revalidation replaces Vercel's cache-tag purge**: every action that should show up on the live site immediately — publish now, schedule promotion, Main Menu save, Settings save, Site Photos upload/delete — calls `revalidatePath()` for the affected marketing pages (`src/lib/publish/service.ts`'s `revalidatePublicMenuSurfaces()`, `src/lib/site/revalidate.ts`'s `revalidateMarketingSite()`). Netlify's Next.js adapter supports on-demand ISR revalidation natively, same as Vercel did. Site Photos uploads revalidate from a small `POST /api/site-media/confirm` called *after* the actual Storage upload completes (not from the upload-target route, which only issues a signed URL and would race the real upload).
- **Daily Special auto-clears at local midnight (2026-07-14, owner's explicit call):** a Daily Special is "today's" board and must clear itself overnight so the site never shows yesterday's special. New column `restaurants.live_since timestamptz` (migration `..._restaurant_live_since.sql`) is stamped on **every** pointer flip — `publishMenuNow` and the cron promotion both set it alongside `live_snapshot_id`. `clearStaleLiveSpecials(admin)` (`src/lib/publish/service.ts`) nulls `live_snapshot_id`/`live_since` once `live_since` lands on an earlier calendar day than "now" in the restaurant's timezone (`menu_defaults.timezone`, optional, default `America/New_York`), comparing `Intl.DateTimeFormat("en-CA", { timeZone })` "YYYY-MM-DD" keys lexicographically. It's called at the end of **every** `/api/cron/promote-schedules` run (the same per-minute Netlify Scheduled Function), so the first tick past local midnight clears it; the route now returns `{ promoted, cleared }`. The snapshot/menu are untouched (stay in History + Library, re-publishable) — only the live pointer clears. Then the homepage falls back to the hero photo, or the `SpecialsPreview` "check back soon" card if no hero (`src/app/(marketing)/page.tsx`: `showSpecialsInHero = hasSpecials || !heroUrl`). Keyed off `live_since` **not** the snapshot's `published_at`, because a *scheduled* snapshot's `published_at` is set at schedule-creation time, not go-live time; specials that went live before this column existed fall back to `published_at`.
- **Netlify deploy**: `netlify.toml` + `netlify/functions/promote-schedules.mts` (a Scheduled Function, `* * * * *`, that just `fetch()`s `/api/cron/promote-schedules` with `CRON_SECRET` — all real logic stays in that route). Required env vars in the Netlify UI: everything in `.env.example`, plus set `NEXT_PUBLIC_SITE_URL` to the real production domain explicitly (Netlify's own injected `URL` var is only used by the scheduled function itself, not this app-facing redirect URL).
- **Deploy prep + real deploy incidents (2026-07-15):** prepped this project for the owner's first manual push to Netlify (owner drives git/GitHub entirely via GitHub Desktop — Claude Code must never run `git`/GitHub commands here, only edit plain files) and fixed three real issues found from actual deploy logs, in order: (1) **`NODE_VERSION` pin** — Next.js 16 requires Node ≥20.9; `netlify.toml`'s `[build.environment]` now pins `NODE_VERSION = "24"` rather than relying on the account/site default, which varies by when a Netlify site was first created. (2) **`.env.example` tripped Netlify's secrets scanner** — it had `SITE_RESTAURANT_SLUG` set to the real production value instead of a placeholder (every other line in that file already used one); Netlify's build-time scanner compares every configured env var's literal value against the repo/build output and fails the build on any match. Not an actual secret (it's a public slug, read server-only via plain `process.env`, never `NEXT_PUBLIC_`-prefixed or exposed via `next.config.ts`) — just a template file that should never contain a real value. Fixed by replacing it with a generic placeholder; confirmed via repo-wide grep it was the only occurrence. **Caution for future edits to this doc:** don't reproduce the literal value here either — the scanner flagged this very file on a later deploy (line 69, this same note) after an earlier draft quoted the real slug value while describing the bug; write around it descriptively instead, the way this paragraph now does. (3) **Netlify's Next.js Runtime auto-detection never activated on this site** — Build settings persistently showed "Runtime: Not set" even after `npm run build` succeeded cleanly and deployed, and the live site 404'd on every path (Netlify's own generic 404 page, not the app's) because nothing wired the `.next` build output into servable functions/routes; `publish` was resolving to the raw repo root as a fallback. **Zero-config auto-detection turned out not to be reliable for this site** (cause unconfirmed — possibly how/when the site was originally created on Netlify) — the originally-documented "no `[[plugins]]` block needed" stance from Phase 2 didn't hold in practice. Fixed by explicitly forcing the runtime per Netlify's own docs for this exact symptom: `@netlify/plugin-nextjs` added unpinned to `package.json` devDependencies (so Netlify still keeps it current) plus a matching `[[plugins]] package = "@netlify/plugin-nextjs"` block in `netlify.toml`. If a future Netlify site for this project (or a second restaurant deployment) shows "Runtime: Next.js" correctly on its own, the explicit plugin block is harmless to leave in place — Netlify docs treat it as a supported way to pin/force the runtime either way. (4) **Even with the plugin forced on, the build still failed**: `Error: Your publish directory cannot be the same as the base directory of your site` — with no `publish` set (the zero-config-recommended default, and also confirmed with the Netlify UI's Publish directory field explicitly cleared), it resolved to the repo root itself (`publish: /opt/build/repo`, matching the base directory), which the plugin's own `onBuild` guard rejects outright — reproduced identically twice, including once after manually selecting "Next.js" from the site's Runtime dropdown (Site configuration → Build & deploy → Build settings), which didn't change this behavior. Fixed by setting `publish = ".next"` explicitly in `netlify.toml`'s `[build]` block — the plugin needs a valid non-base starting directory before it takes over and transforms the actually-served output itself. (5) One more secrets-scan hit, self-inflicted: an earlier draft of item (2) above quoted the real slug value while narrating the bug, and Netlify's scanner correctly flagged this very file on the next deploy. Rewritten to describe it without reproducing the value — **do not paste real secret/config values into this file even when explaining a historical bug; describe them instead.** **Outcome: deploy succeeded** — build, functions bundling, edge functions bundling, and the secrets scan all passed clean, and the live site now renders correctly (confirmed working, not just deployed-with-no-errors) at the Netlify site the owner already had provisioned, `kkmainweb.netlify.app`. Every fix above stays in `netlify.toml`/`package.json` going forward; none of it is a one-time-only workaround that should be reverted later.
- **Library repurposed to "Saved Specials" (2026-07-16, owner's call):** `/admin/library` no longer shows the item_library — it's now a browsable grid of **every rendered Daily Special** (any `menus` row with a `generated_image_path`, drafts included), so a rendered-but-unpublished special is never lost. `src/lib/menu/saved-specials-service.ts` (`listSavedSpecials` badges the currently-live one; `getLatestDraft` finds the most recent unpublished draft with `special_data`). The New Daily Special screen (`new/page.tsx` is now a server wrapper → `new-special-client.tsx`) shows a **resume-in-progress banner** from `getLatestDraft` so an extracted-but-unrendered draft is also recoverable. `deleteMenu()` now also removes the rendered `.svg` from `site-media` so Library deletes don't orphan files. The old item-library **UI/service/schema/API routes were deleted** (fully orphaned since the OCR pipeline's removal); the `item_library` table + `ItemLibraryEntry` type remain (vestigial, documents the table).
- **Post-cleanup verification (2026-07-15):** after the widget/OCR-pipeline removal above, ran a full sweep — `tsc --noEmit` and `eslint .` both clean, grepped the whole repo for stale `widget`/`embed`/`hosted page`/`Compare mode`/`ocr_parse` mentions (all remaining hits are deliberate past-tense history, e.g. `src/proxy.ts` and `src/lib/themes/tokens.ts` say "retired"/"widget-era" explicitly), and spot-checked every admin + public route live in the browser. Found and fixed one real regression the grep sweep alone wouldn't have caught: `/admin/library` still said "No items learned yet — they accumulate as you save reviewed menus" (`src/app/admin/library/page.tsx`, `library-list.tsx`) — stale because `learnFromSections`/fuzzy-suggest were deleted with the OCR pipeline; the page is view/manage-only now (docs/03/docs/07). Rewrote both to say so. All docs (01–08) and README.md were also brought in line with current behavior in this pass.
- **Admin is tablet-first + mobile-responsive (2026-07-14, owner's call):** the `/admin` back-end is designed for tablets first, degrading gracefully to phones. Shared shell is `src/app/admin/admin-nav.tsx` (client) rendered by `layout.tsx`: a sticky header with the full inline nav row visible from the `lg` breakpoint up, collapsing to a hamburger drawer below `lg`, with active-page highlighting via `usePathname`. (Originally `md`, but once Sign out was added the 7 items wrapped/crammed at 768px — moved to `lg` 2026-07-15 so tablets get the clean drawer; links are `whitespace-nowrap` so they never break mid-label.) Per-screen fixes in the same pass: Main Menu item row is a name-wide `sm:grid-cols-[minmax(0,2fr)_minmax(0,1fr)_minmax(0,1.5fr)]` (name spans both cols on mobile); Settings hours rows `flex-wrap` so the two `type=time` inputs + Closed checkbox never overflow a phone; Site Photos gallery is `grid-cols-2 sm:grid-cols-3 md:grid-cols-4` and its per-photo Delete button is **always visible** (was `opacity-0 group-hover:opacity-100` — unreachable on touch tablets); Review screen's Featured/Soup/Veggie micro-grids stack (`grid-cols-1 sm:grid-cols-3`). **Tailwind gotcha fixed here:** the Review entrée/combo/dessert rows had the name field crushed to ~18px because the price input carried both `w-full` (from the shared `field()` helper) and `w-24` — conflicting width utilities let the price box resolve to 100% and starve the `flex-1` name beside it. Fixed with a dedicated `priceField()` class that omits `w-full`. When putting a `field()`-style input (which includes `w-full`) into a flex row next to a fixed-width sibling, don't also add a `w-*` to the sibling — give it a `w-full`-free class instead. Verified at 375px and 768px across every admin screen; `tsc`/`eslint` clean.
- **Review & Publish is preview-first + accordion editor (2026-07-14, owner's call):** the Review screen (`src/app/admin/menus/[id]/review/review-special-client.tsx`) is a single vertical column (the old `lg:grid-cols-2` editor|preview split is gone; `review/page.tsx` is now `max-w-3xl`). Order top-to-bottom: **Style picker → live SVG preview → "Save & render" → PublishBar** (the output/actions, shown first), then an **"Edit the menu"** group where every editable section (Header, Entrées, Featured, Soup, Combos, Veggie, Desserts, Sides) is a **collapsed-by-default `Accordion`**. Each accordion header shows a one-line summary of its collapsed content (`itemCount()` for the list sections, the field value for singletons) and an amber attention dot when the section needs review (Entrées when any entrée is low-confidence). The live preview + `dirty`/publish gating are unchanged — editing inside an accordion still updates the top preview and re-arms "Save & render". The `uncertainItems` warning banner sits just above the accordions.
- **Auth is email + password, not magic link (2026-07-14, owner's call):** the owner asked to drop the magic-link flow (and its email-redirect/allow-list deployment overhead). `/login` (`src/app/login/page.tsx` + `actions.ts`) is now a plain email + password form calling `supabase.auth.signInWithPassword`, which sets the session cookie via the server client; on success the action `redirect("/admin")`. The magic-link `/auth/callback` route was **deleted** (was `exchangeCodeForSession`), and `/auth/callback` removed from `src/proxy.ts`'s matcher and `middleware.ts`'s `PUBLIC_PATHS`. The owner user is created in the Supabase dashboard (**Auth → Users → Add user**, email + password, Auto Confirm) and linked with `supabase/seed/bootstrap_owner.sql` — there is **no public sign-up** in the app. `restaurant_members` (authorization) is still separate from authentication: inserting that row grants admin authority but you still sign in to get a session. **`NEXT_PUBLIC_SITE_URL` no longer affects auth** — its only remaining consumer is the Netlify cron function's local `netlify dev` fallback (`process.env.URL` is used in production), so it doesn't need to be set on Netlify. The `restaurants`/`published_snapshots` middleware guard and RLS are unchanged. To migrate an existing magic-link owner: set a password on their existing `auth.users` row in the dashboard (keeps the same user id, so their `restaurant_members` link stays intact).
- **Username login + self-service account + employee role (2026-07-14, owner's call):** three related additions on top of email+password auth.
  - **Username-or-email login:** `restaurant_members.username` (migration `..._member_username.sql`, stored lowercased, unique on `lower(username)`). `src/app/login/actions.ts` accepts a single `identifier` field — if it has no `@` it resolves username → account email via the **service-role** client (no session yet, so RLS would hide the row), then `signInWithPassword`. Failures stay generic (no enumeration).
  - **Self-service account (owner):** Settings → **Account** panel (`src/app/admin/settings/account-panel.tsx` + `account-actions.ts`) lets the signed-in user change their **username** (service-role update after verifying the session user, since `restaurant_members` has a self-SELECT but no UPDATE RLS policy; uniqueness checked) and **password** (`supabase.auth.updateUser`). Shows current email + username.
  - **Employee role:** `restaurant_members.role` now allows `'employee'` (migration `..._member_roles.sql` widened the check constraint) in addition to `'owner'`. Employees are gated in `src/lib/supabase/middleware.ts` to **only** the Daily Special generator — allowed prefixes `/admin/menus`, `/api/menus`, `/api/uploads`, `/api/schedules` (plus bare `/admin`); everything else under `/admin` redirects to `/admin/menus/new` and every other authenticated `/api` returns 403. `admin-nav.tsx` filters to just the "New Daily Special" link for employees (`role` prop from the layout). **The role boundary lives in the middleware, not in RLS** — RLS is member-level, so any member (owner or employee) can write the restaurant's menus/snapshots; the middleware is what stops an employee reaching Settings/Main Menu/Site Photos/etc. Add an employee with `supabase/seed/add_employee.sql` (create the auth user in the dashboard first, then run it — sets role=employee, username=employee). A **Sign out** control (`src/app/admin/sign-out-action.ts` → `supabase.auth.signOut()` + redirect to `/login`) sits in the admin nav for every role — in the desktop row (right, after a divider) and at the bottom of the mobile drawer — so owner and employee can switch accounts on a shared device.
- **Menu page's category jump-nav no longer wraps (2026-07-15):** the sticky category bar (`src/app/(marketing)/menu/page.tsx`, `sticky top-14`, links to `#specials`/`#breakfast`/etc.) used to be `flex flex-wrap justify-center`, a full-width bar — on a phone too narrow to fit all labels in one row, it wrapped to 2 rows, roughly doubling its height. The section headings' `scroll-mt-28` (112px, `CategoryHeading`) was sized for the one-row case, so after tapping a jump link the target heading landed **behind** the now-taller wrapped bar (reported live: tapping "Today's Specials" hid that exact heading under the bar). Fixed by making the bar a shrink-to-fit, single-row, horizontally-scrollable chip strip instead of ever wrapping: `flex w-fit max-w-full flex-nowrap items-center gap-2 overflow-x-auto` + `mx-auto` (was `flex flex-wrap justify-center`, full-width), each chip `shrink-0 whitespace-nowrap`, scrollbar hidden via the new `.no-scrollbar` utility (`globals.css`, same technique as `.review-marquee-viewport`) while native touch/wheel scroll still works. Bar height is now a constant 50px at every breakpoint, so the existing `scroll-mt-28` is correct everywhere — verified live via `getBoundingClientRect` at 320/375/768/1280/1920px that every one of the 4 jump links lands the target heading's top flush against the bar's bottom edge (0px gap at ≤768px where the bar is genuinely sticky-adjacent; a small fixed 6px breathing gap at desktop widths where the bar shrink-wraps well under the content column's width) — no overlap anywhere. Bonus: at tablet/desktop widths the bar now shrink-wraps tightly to its content instead of stretching full-width with the labels centered inside a mostly-empty pill — a tighter, more deliberate look consistent with the "don't spread things out for no reason" preference from the History-page fix earlier.
- **...and that scrollable strip's trailing chip no longer hard-clips (2026-07-15, same-day follow-up):** the horizontal-scroll fix above introduced a new, smaller defect — on a narrow viewport, a chip only partially scrolled into view (e.g. "Lunch & Dinner" showing as "Lun") got a hard text clip at the pill's rounded edge, reading as a text-overflow bug rather than an intentional "scroll for more" affordance (reported live via screenshot). Fixed with a new `.scroll-fade-x` utility (`globals.css`, `mask-image: linear-gradient(to right, transparent, black 20px, black calc(100% - 20px), transparent)`) applied alongside `.no-scrollbar` on the same nav — the trailing/leading chip now fades to transparent instead of stopping dead. The 20px fade width sits inside the pill's own edge padding (8px bar padding + 12px chip padding ≈ 20px), so at desktop widths where the bar doesn't overflow at all, the fade is invisible — confirmed live via screenshot at 1920px that all 4 labels render fully intact with no visible fade artifact, and via computed style (`getComputedStyle(nav).maskImage`) that the gradient is applied.
- **...and the 4th jump-nav chip ("Beverages") now fits without scrolling at all (2026-07-16, reported live as "missing"):** the two fixes above made the bar scroll rather than wrap; with only 3 categories that was invisible, but a 4th chip pushed the row past a phone's width so Beverages sat mostly scrolled off-screen and read as *absent*. **Wrapping to a 2nd row was explicitly not an option** — that's the exact bug the first fix above cured (a taller sticky bar breaks every `scroll-mt-28` heading offset below it), so the fix had to come from making the content narrower, not the container taller. Tightened the chips below `md` only (`gap-0`, nav `px-0.5`, chip `px-1`, `text-xs`); `md`+ keeps the original comfortable sizing (14px text, 32px chips) since a real tablet has room to spare. At 375px this took the row from ~150–185px of overflow to **0** — all 4 chips fit, symmetric 23px gutters. 320px (iPhone SE) still overflows ~50px and correctly falls back to the existing scroll+fade; 4 full-word chips genuinely can't fit that width without going tighter than is tappable. Verified 320/375/768/1280, no page overflow at any. **Also tried and reverted:** letting the bar bleed past the page's own `px-5` via `max-w-[calc(100%+1.5rem)]` to claw back ~24px — measured live, it broke centering and caused real page overflow (`mx-auto`'s even-split only holds when content actually fits), so the bounded `max-w-full` stayed and the win came from spacing alone.
- **Daily Special image display: lightbox + thumbnail crop (2026-07-16, two separate live reports):** both are about the *rendered SVG* (1000px wide, height driven by content — a full board runs ~1100px+, i.e. portrait), and both came from a fixed-aspect container fighting a tall image. (1) **Lightbox was too small on mobile** (`menu/special-image.tsx`): it force-fit the whole menu into `max-h-[95vh]` **and** `max-w-[95vw]` with no scrolling, so a long board shrank to whatever made it fit entirely on one screen — legible only in theory. Replaced with full-width + `overflow-y-auto` (the pattern `SpecialsPreview`'s own lightbox already used): the image renders at full device width and you scroll it, which is strictly more readable than "the whole thing at once, too small to read," and scales with board length instead of degrading as the menu grows. Desktop (`sm:`+) keeps its `max-w-2xl`/95vh/rounded/padded treatment unchanged. Verified: 375px → image 356px → 375px wide, wrapper `overflow-y: auto`; 1280px unchanged; close button + no page overflow both still fine. (2) **Homepage thumbnail cropped the wrong end** (`specials-preview.tsx`): the card forces `aspect-[4/3]`/`lg:aspect-square` with `object-cover`, whose default centered crop cut off the top — the letterhead, title, and date — and started mid-item-list. One-word fix: `object-top`. Verified via the live SVG's own `<text>` y-coordinates that the restaurant name (y=68), address/phone, and "Daily Special Menu" (y=146) all now fall inside the visible window; anchoring to y=0 is robust to any board length since it only ever crops the bottom.
- **Nav stability + page transitions (2026-07-15, owner's call):** the public site's header was subtly shifting between pages — traced to the active nav link's `fontWeight: 600` (vs `400` inactive), which changes a link's rendered width, nudging every link after it a few px on each navigation. Fixed in `src/app/(marketing)/site-nav.tsx`: font-weight is now constant (`font-medium`) for every link, and the active indicator is a `boxShadow: inset 0 -2px 0 0 <color>` underline instead — box-shadow paints color only and never occupies layout space, so no link ever changes width. Verified live: the logo and "Call to Order" CTA sit at the exact same pixel position on every page (measured via `getBoundingClientRect`), and the "Menu" link measures identically whether active or not. The one thing that's still supposed to move is the "Home" link itself (appears only when `pathname !== "/"`) — that's the explicit, requested exception. Also added `html { scrollbar-gutter: stable }` (`globals.css`) so a page tall enough to need a scrollbar vs. one that isn't no longer shifts all centered content sideways by the scrollbar's width — a common, easy-to-miss cause of "the header moves." **Page transitions:** `src/app/(marketing)/template.tsx` and `src/app/admin/template.tsx` each wrap `{children}` in a `.animate-page-in` div (`globals.css`: 220ms `ease-out` fade + 6px slide-up, `prefers-reduced-motion` disables it). Deliberately plain CSS, no new dependency (no framer-motion) and no reliance on Next's still-experimental View Transitions API integration — `template.tsx` is the standard, stable Next.js App Router mechanism for this: it remounts on every navigation while `layout.tsx` (the nav/footer) does not, so the transition fires purely on the page content and the nav never re-renders, flashes, or resets. Confirmed via computed styles that the animated wrapper and the `<header>` are disjoint DOM subtrees on both the public site and `/admin`.
- **Public site header goes hamburger-only below `lg`, three responsive tiers (2026-07-16, reported live via annotated screenshot):** at a mid-width viewport all 7 nav links + the logo + the EN/ES toggle + the "Call to Order" CTA were forced into one row — the logo wrapped to 2 lines and the CTA button visibly broke/wrapped. Same crowding class of bug admin-nav hit earlier with far fewer items (its own dated note above), same fix: moved the collapse threshold from `sm` (640px) to `lg` (1024px), verified live at 1023/1024px that the switch is clean. Below `lg` the CTA never wraps regardless of width (`shrink-0 whitespace-nowrap` on every instance of it, plus the logo). Owner also asked that the EN/ES toggle and CTA stay visible in the top bar (next to the hamburger) rather than immediately buried in the drawer — the phone-order CTA is the site's actual ordering mechanism pre-online-ordering, so it needs to stay one tap away down to a real "very thin" floor, only then joining the drawer. That floor is `min-[460px]` (an arbitrary Tailwind breakpoint, not one of the named ones) — **deliberately not the ~400px a first pass measured in English alone**: Spanish's CTA copy ("Llame para ordenar") is ~45px wider than English's ("Call to Order"), and 430px produced real, measured header/page overflow in Spanish (`headerOverflow: 14px`, `pageOverflowX: true`) despite being clean in English. 460px is verified overflow-free in both languages at the boundary (459px correctly collapsed, 461px clean in both languages) — this project's now-standard lesson of testing every EN/ES-affecting layout in both locales, not just English, applies here too; also tried a `max-w-[calc(100%+…)]` bleed trick to claw back space from the page's own side padding for the earlier /menu jump-nav fix (see its dated note) and it's *not* used here either, for the same reason: unreliable centering under real content.
  Two same-day follow-ups from further live-reported screenshots, in order: (1) **drawer text right-aligned** (`text-right` on every link, rows stay full-width so the tap target doesn't shrink) — a left-hugging drawer list read as misaligned against its own trigger, which lives on the right with everything else (toggle, CTA, hamburger). (2) **drawer is a floating overlay, not in-flow** — opening it was pushing the whole page down instead of covering it; `header`'s existing `position: sticky` already serves as a positioning context, so the drawer became `position: absolute`, anchored `top-full`, with its own opaque background + shadow (no longer physically inside the header's painted box once taken out of flow) and a `max-h-[calc(100dvh-4rem)] overflow-y-auto` safeguard. Verified live via `getBoundingClientRect` that the hero heading's position is pixel-identical whether the drawer is open or closed, and via `document.elementFromPoint` that the drawer actually paints on top of the hero content rather than just geometrically overlapping it. (3) **drawer is a compact right-anchored dropdown, not a full-width bar** — combining (1) and (2) initially still spanned the full viewport width with only the *text* right-aligned inside it, leaving a large blank rectangle to the left of a narrow text column (reported live, again via annotated screenshot). Changed `inset-x-0` to `right-6` (lines its edge up with the hamburger button, matching the top bar's own `px-6`) plus a fixed `w-64` (`max-w-[calc(100vw-3rem)]` safety cap for narrow screens) and swapped the full-bleed `border-t` for a proper `rounded-xl border` card. Re-verified in both languages: the longer Spanish CTA fallback text wraps to 2 lines inside the narrower card (56px vs English's 36px) — acceptable and expected for this block-style drawer button, unlike the top-bar pill CTA's hard "never wrap" requirement, which is enforced separately via `shrink-0 whitespace-nowrap`.
- **Dropdown menus are right-aligned, compact, and float — a general rule (2026-07-16, owner's explicit call: "Anytime we use a drop down menu like this, make sure the text objects for the menu are centered to the right"):** the three properties the public `SiteNav` drawer arrived at (see the note directly above) are now the standard for **every** dropdown menu in the app, not one component's quirk. Any new dropdown should get all three from the start: (1) `text-right` on every row (rows stay full-width so tap targets don't shrink — align the *text*, not the row); (2) right-anchored + fixed width (`right-N` matching the container's own horizontal padding so the card's edge lines up with its trigger, plus a `max-w-[calc(100vw-…)]` clamp) rather than a full-width bar with a narrow text column stranded inside it; (3) `position: absolute` + `top-full` over a `position: sticky` header, with its own background/shadow and a `max-h`/`overflow-y-auto` safeguard, so opening it floats over the page instead of pushing content down. **Applied to `src/app/admin/admin-nav.tsx`'s drawer this pass** — it had all three of the original defects (including a literal `text-left` on Sign out, and the in-flow push-down visible in the owner's screenshot). Verified live at 375/320px, EN + ES: text-align `right` on all 7 links *and* Sign out, 224px card with a 16px gutter matching the header's `px-4`, main heading pixel-identical (125px) open vs. closed, `elementFromPoint` confirms it paints over page content, Spanish's longer labels ("Nuevo especial del día") still fit on one line, no page overflow at either width. **Swept the rest of the app the same pass — no other true dropdown menus exist:** `menu-item-image-popover.tsx` is a *photo* popover (no text rows to align, and it's deliberately left-anchored because its trigger — the item name — is itself on the left, see its own note); `main-menu-editor.tsx`'s category `<select>` is a native browser control whose option list isn't CSS-addressable; and `SpecialsPreview`/`SpecialImage`'s lightboxes and the Review screen's accordions are centered modals / expanding form sections, not dropdown menus. So the rule's live surface is exactly these two navs, now consistent.
- **Visit/Gallery/Catering mastheads centered too; Order was already fine (2026-07-15):** finished the centering sweep across the remaining public pages. `order/page.tsx` needed nothing — its `<main>` already carries `text-center` and its CTA row already has `justify-center`, fully consistent already. `visit/page.tsx`: centered the standalone "Visit Us" H1 only — the 2-col info grid below it (address/Get Directions on the left, the hours table on the right) stays left-aligned, correctly, since neither an address nor a day-by-day hours table should be centered (same reasoning as leaving About's body prose and Menu's item-price rows alone). `gallery/page.tsx`: centered the H1, the one-line description, and the closing "Hungry? See the menu →" line; the photo grid itself was untouched (it already fills the container edge-to-edge). `catering/page.tsx`: centered the kicker+H1 masthead (matching About) *and* the "Let's plan it" mini-CTA block (heading + intro line + the Call/Email button row) near the bottom — extending the precedent that Menu already centers every section heading, not just the page's very top one — while leaving the body paragraphs and the "What we offer" card entirely untouched (card-internal content reads left, matching Home's highlight cards). Verified live at 375/843/1280px on all four, zero overflow.
- **About page's masthead centered to match Home/Menu (2026-07-15):** checked all three content-heavy public pages for the same "some elements centered, others not" mismatch just fixed on Home. Menu page was already fully consistent (H1, jump-nav chips, every `CategoryHeading`, and the bottom CTA row are all already centered; only the item-name/price rows are left-aligned, correct for a receipt-style list) — no changes. About page was internally consistent with itself (kicker/H1/lead/body/CTA all uniformly left) but stylistically out of step with Home/Menu's now-centered treatment, and unlike Home it's a single-column layout at every width (no `lg:` side-by-side split), so centering here needed no responsive variants. Centered the kicker, H1, and lead line (`text-center`) and the CTA row (`justify-center`) — deliberately left the multi-paragraph body prose left-aligned, since centering flowing paragraph text is a real readability regression, not a style choice. Verified live at 375/843/1280px.
- **Hero is centered while stacked, left-aligned once side-by-side (2026-07-15):** below `lg` (the hero's single-column stacked state — everything from real mobile up through ~1023px, including the ~843px width this was reported at), the intro text (kicker/h1/tagline), the hero body paragraph, the CTA button row, and the hours badge were all left-aligned by default while the specials-menu card/photo next to them was already centered (`mx-auto`) — inconsistent with each other on the same page. Added `mx-auto text-center` (kicker/h1/tagline block and the body paragraph) and `justify-center` (the CTA button row, plus `text-center` on its wrapper so the hours badge inherits centered alignment) to each of those pieces, all reverting via `lg:mx-0 lg:text-left`/`lg:justify-start` at `lg:` where the hero becomes a 2-col grid (text beside the art) and left-alignment reads naturally there instead. Verified live at 843px (screenshot match), 375px (mobile), and confirmed 1280px reverts to the original left-aligned/side-by-side desktop look, unchanged.
- **Homepage sections spread out more at desktop/ultrawide (2026-07-15):** every homepage section (hero, highlights, visit strip) was capped at `max-w-5xl` (1024px) at every viewport width — on a real ultrawide monitor that left huge empty margins on both sides while the reviews marquee (already deliberately full-bleed, see the ultrawide-responsiveness pass) filled the entire width right in between, a jarring contrast. Widened those three sections' containers with `xl:max-w-6xl 2xl:max-w-7xl` (1024 → 1152px at 1280px+ → 1280px at 1536px+, capped there — not full-bleed, still a sensible reading/scanning width even at 2560px+) and bumped the internal grid gaps to match (hero `xl:gap-x-16`, highlights `xl:gap-8`, visit-strip `xl:gap-12` + `xl:px-12 xl:py-14`) so the extra room shows up as genuine breathing space, not just wider outer margins with the same cramped internal spacing. Verified live via `getBoundingClientRect`: 1152px at 1280px viewport, 1280px at both 1920px and 2560px (confirmed stable, not still growing), zero page overflow at any width, and confirmed mobile (375)/tablet (768) completely unaffected (`xl:`/`2xl:` only). Deliberately left `reviews-section.tsx` untouched (still `max-w-5xl`) since it's shared with the About page and the ask was scoped to the home page — the reviews heading being narrower than its neighbors is a minor, low-visibility inconsistency (short centered text, not a paragraph block) rather than something worth widening a shared component for without being asked.
- **Footer is slim at desktop/ultrawide (2026-07-15):** the footer's 3-column grid (`src/app/(marketing)/layout.tsx`) was unnecessarily tall on wide screens because its middle column (6 nav links) and its bottom-right legal block (icons/copyright/Privacy+Terms/Staff sign-in) each stacked vertically regardless of viewport width — 6 lines and 4 lines respectively, versus the brand column's 3 lines, so the whole footer's height was governed by those two stacks even when there was ample horizontal room. Fixed with `lg:` variants only (mobile/tablet's already-reasonable 3-column stack is untouched below 1024px): the nav becomes `lg:flex-row lg:flex-wrap` (6 links on one row), and copyright + Privacy/Terms + Staff sign-in collapse from 2 lines to 1 (`lg:flex-row lg:items-center lg:gap-3`); `lg:items-center` on the grid vertically centers the now-similar-height columns instead of top-aligning; `lg:py-8` trims the vertical padding a bit further (was `py-12` at all sizes). Verified live: footer height dropped from 257px (tablet, unchanged) to 145px at both 1280px and 2560px viewports — confirmed the exact same 145px at both, i.e. stable regardless of extra width, not stretching taller. Mobile (375px) confirmed unchanged (nav still `flex-direction: column`, 6 links on separate rows). The full-bleed edge-to-edge spread of the 3 columns at ultrawide (brand hugging the far left, legal hugging the far right) is unchanged and intentional — matches the owner's earlier explicit "stretch the header... and the footer in the bottom left and right" request from the ultrawide-responsiveness pass.
  **Same fix extended down to tablet, 2026-07-16 (reported live, unnecessarily tall at 768px):** the `lg:` (1024px) trigger above was conservative in the same way the SiteNav's old `sm` hamburger threshold and the /menu jump-nav's old chip sizing were (see their dated notes) — real tablet widths had plenty of room this whole time, just never re-checked. Moved both the nav (`lg:flex-row lg:flex-wrap...` → `sm:flex-row sm:flex-wrap...`) and the legal line (`lg:flex-row lg:items-center lg:gap-3` → `sm:flex-row sm:flex-wrap sm:items-center sm:justify-end sm:gap-3`, added `sm:flex-wrap` since 6 links can't always fit one row this much earlier — it wraps to 2 balanced rows instead of 6 single-item ones, still far shorter) down to `sm` (640px), matching the same breakpoint the 3-column grid itself already switches on. Verified live at every width in the standard sweep: 375px unchanged (495px, still genuinely too narrow for anything but a full stack, matches the original rationale for *that* width), clean transition at the 639px→640px boundary (495px → 197px), 768px (the reported width) dropped 257px → 197px, 1024/1280/2560px unchanged from the existing figures (165px / 145px / 145px) confirming no regression at the sizes that already worked — and re-verified in Spanish (longer nav labels/legal text) at 640px and 768px, both clean (203px vs English's 197px, columns still balanced, no overflow).
- **Site-wide overflow sweep (2026-07-15):** after the two menu jump-nav fixes above, swept every public page (`/`, `/menu`, `/about`, `/visit`, `/order`, `/gallery`, `/catering`, `/privacy`, `/terms`) at 320/375/768px for actual horizontal page overflow (`document.documentElement.scrollWidth > clientWidth`, ignoring the two known-intentional scroll strips: the reviews marquee and the menu jump-nav), plus opened both `fixed`-positioned modals (`SpecialsPreview`'s lightbox, `SpecialImage`'s lightbox) at each width — all clean, no page-level overflow found anywhere else. Did find and fix one real (currently dormant) bug of the same "floating element not clamped to the viewport" class: `src/app/(marketing)/menu/menu-item-image-popover.tsx`'s photo popover used `left-1/2 -translate-x-1/2` (centered on the trigger) — for a short item name near the start of a row (confirmed live with a synthetic DOM test: a 3-letter name at the page's left padding), the popover clipped ~20px off the left edge of the viewport, silently cutting off part of the photo with no way to scroll to it. Fixed by anchoring `left-0` instead (no centering) — safe because every item name in `ItemRow` always starts at the same left x (name is always the first child in the row), so the popover can never need to extend left of its trigger. No menu items currently have photos uploaded so this was unreachable in practice, but it's a live feature (`/admin/main-menu`'s per-item image upload) so it'll trigger the moment an owner adds one.
- **Owner-managed employee accounts (2026-07-15):** employees have no self-service Settings access (middleware blocks `/admin/settings` for them entirely — confirmed live: an employee session hitting that path redirects to `/admin/menus/new`, and every non-generator API like `/api/restaurant`/`/api/providers/keys`/`/api/history`/`/api/site-media` returns 403), so the owner manages employee credentials **from their own Settings page** instead of the Supabase dashboard. `src/lib/restaurant/employees-service.ts`'s `listEmployeeAccounts(restaurantId)` uses the service-role client to list every `role='employee'` member of the restaurant with their auth email (`restaurant_members`' self-SELECT RLS policy would otherwise hide every row but the caller's own). `src/app/admin/settings/employee-actions.ts` exposes `updateEmployeeUsername`/`updateEmployeeEmail`/`updateEmployeePassword`, each gated by `assertOwnerManagesEmployee()` — re-verifies (defense in depth, not just relying on the route-level middleware block) that the **caller** is `role='owner'` and the **target** user is an `employee` of that same restaurant, since these actions use the service-role client to modify another user's `auth.users` row directly (`admin.auth.admin.updateUserById`). Rendered as `employee-panel.tsx`'s `EmployeePanel` in Settings, below the owner's own `AccountPanel`, only when `listEmployeeAccounts` returns at least one row. Verified live end-to-end: signed in as the employee, confirmed the nav shows only "New Daily Special" + Sign out and every owner-only API 403s; signed in as owner, confirmed the panel lists the employee, changed their username through the UI (round-tripped and reverted), and confirmed the uniqueness guard correctly rejects reusing an already-taken username (e.g. the owner's own).
- **Public site Spanish translation, step 1 — site chrome + toggle (2026-07-15):** the public site (not `/admin`, which stays staff-only English) is now bilingual EN/ES for everything that's code-owned text. Deliberately **cookie-based, not `/en`/`/es` URL prefixes** — no route restructuring, no new dependency, in keeping with docs/01's low-maintenance stance; trade-off noted in docs/06 (Spanish pages aren't independently indexable by URL yet). `src/lib/i18n/locale.ts` holds the client-safe `Locale` type/constants (no `next/headers`, so client components can import it); `src/lib/i18n/get-locale.ts` holds the server-only `getLocale()` cookie read — **keep these separate**, an earlier draft merged them and broke every client component that imported the type (Next.js refused to bundle `next/headers` into client code). `src/lib/i18n/dictionary.ts` holds short UI-atom strings (nav, buttons, days) keyed `en`/`es`, typed via an explicit `Dictionary` interface rather than `as const` inference (the two locale objects don't share literal string types, only shape). `src/lib/site/content.ts` (long-form prose: hero, about, catering, highlights, reviews) is now keyed the same way via `getSiteContent(locale)` — the plain `siteContent` export is gone, every call site now threads `locale`. The toggle itself lives in `SiteNav` (a compact EN/ES pill, both in the desktop header and the mobile top bar next to the hamburger — reachable without opening the drawer) and writes the `site_locale` cookie via a plain module-level `setLocaleCookie()` helper (`src/lib/i18n/set-locale-cookie.ts`) rather than a direct `document.cookie = ...` inside the component body — the latter trips `react-hooks/immutability` (React Compiler), the same class of false-positive as the `static-components`/`rules-of-hooks` quirks noted above. Review quotes in `content.ts` are translated too (not left English) — presented as the site's own summary for Spanish readers, not a claim the reviewer wrote in Spanish. Main Menu item names/descriptions (owner-typed, in Supabase) and Daily Specials are **out of scope for this step** and still render in whatever language they were entered/extracted in — see docs/06 and docs/08 for the proposed next-step design for both.
- **Main Menu Spanish translation, step 2 (2026-07-15):** `TextProvider` (docs/05) was interface-only before this — defined but never implemented by any adapter, despite docs claiming "text ✅" for all three vendors. Implemented for real in `openai-compatible.ts` (covers OpenAI + xAI, which both wrap it): `generate()` (plain chat completion) and **`generateJson()`**, the text-only counterpart to vision's `extractJson` — same strict `json_schema` mechanism, no image. New `translate_menu` provider task (default OpenAI `gpt-4o-mini`) backs a **"Translate to Spanish"** button in `/admin/main-menu` (`main-menu-editor.tsx`): translates whatever section/item `name`/`description` doesn't already have a `name_es` (so re-running only fills gaps, never clobbers an owner's manual correction), in **parallel batches of 40** (`src/lib/main-menu/translate-service.ts`) keyed by the client's own row `key` rather than array position — safe against truncation/reordering on a real ~200-item menu (Kountry Kitchen's actual menu; verified live end-to-end, translated all 266 section+item units in one run, translation quality is natural and correctly leaves prices/price-notes/numbers untouched). A **"Show Spanish fields"** toggle reveals amber-tinted editable ES inputs for owner review before Save — nothing reaches the public site untranslated-and-unreviewed. **Real incident while shipping this:** `replace_main_menu()` deletes and reinserts every row on every save, so the editor must resend `name_es`/`description_es` on *every* save (even a plain English-only edit) or translations silently vanish — easy to miss since nothing errors when it happens. Separately, adding `name_es`/`description_es` to `public-main-menu/service.ts`'s `.select()` string **before** the migration had actually been run against the live Supabase project broke the public `/menu` page's entire Main Menu section (query failed, section silently rendered empty — Daily Specials and the rest of the site were unaffected). Caught live via console errors, not proactively — worth remembering: a `.select()` naming a new column is a hard runtime dependency on that migration being applied first, with no compile-time signal that they're out of sync. This project's migrations are run by hand via the Supabase SQL editor (no CLI/DB password available, per the local-dev memory note), so that ordering has to be tracked manually, not assumed to be automatic. Public `/menu` picks the Spanish field per-section/per-item when the visitor's locale is `es` and one exists, falling back to English otherwise (`menu/page.tsx`'s `localize()`/`localizeNullable()`).
- **Daily Special Spanish translation, step 3 (2026-07-15):** same `translate_menu`/`generateJson` machinery as Main Menu, adapted to the `DailySpecialMenu` shape and a single-draft scale. `src/lib/menu/translate-special-schema.ts` flattens the menu into `{id, text}` units keyed by a plain path string (`"entrees.2.description"`, not a DB id — there's no per-row id at this granularity) rather than reusing Main Menu's `{id, name, description}` shape; deliberately **not** touching `restaurantName`/`address`/`phone` (facts) or any price field. One `generateJson` call covers the whole board — no chunking needed at this size, unlike Main Menu's batches of 40. New columns `menus.special_data_es`/`generated_image_path_es` (migration `20260706000026`). The Review screen (`review-special-client.tsx`) grew a parallel `specialEs` state + `editEs()` mutator alongside the existing `special`/`edit()`; array add/remove (entrées, combos, desserts, sides) needed dedicated `addEntree()`/`removeEntree()`-style helpers that touch *both* languages' arrays together, or English/Spanish would drift out of index-alignment the moment the owner added or removed a row after translating. "Translate to Spanish" here **always fully retranslates** (relabels to "Re-translate to Spanish" once a translation exists) — the opposite of Main Menu's "only fill gaps" rule, and a deliberate difference: at ~10–40 fields for one day's board, always-fresh-and-in-sync-with-English beats defending partial manual edits, unlike Main Menu's ~200-item catalog where losing accumulated corrections would actually hurt. `renderAndStoreSpecial()` now renders+stores both images together in one call whenever a Spanish draft exists (same theme, same deterministic renderer, just different input text — "AI reads, app draws" holds for the Spanish artifact too), and leaves a previously-saved Spanish render alone when `menuEs` is omitted, so an English-only re-render can never silently delete an existing translation. Publish payload gained `imageUrlEs` next to `imageUrl`; the public site's `localizedSpecialsImageUrl()` (`public-menu/service.ts`) picks between them by visitor locale, falling back to English. Applied the ordering lesson from the Main Menu incident above: this time no public-facing query names the new columns explicitly (admin reads go through `select("*")`, and the public site reads `payload.menu.imageUrlEs` from already-materialized snapshot JSON, never the `menus` table directly) — so shipping this code ahead of the migration only risked the new "Save & render" action erroring with a clear message, not breaking anything already live.
- **`/admin` backend translation, step 4 (2026-07-15):** the three steps above translate what customers see; this one translates the staff tool. Scoped to exactly the **employee-accessible surface** — login, `AdminNav`, Capture, Review & Publish — since employees are already middleware-gated to that same set of routes (docs/04); Settings/Main Menu/History/Site Photos are owner-only screens and stay English, a deliberate scope cut, not an oversight. Reuses the *same* `site_locale` cookie as the public toggle — one language preference for the whole app, no separate admin toggle state. `AdminNav` (`src/app/admin/admin-nav.tsx`) got the identical EN/ES pill pattern as `SiteNav`. `src/app/login/page.tsx` had to be split — it was a single `"use client"` component, and client components can't call the server-only `getLocale()` — into a server `page.tsx` (reads the cookie) rendering a new `login-form.tsx` client component (takes `locale` as a prop); `actions.ts`'s `signIn` server action localizes its own two error messages by calling `getLocale()` directly (Server Actions run server-side, no prop-threading needed there). Added a **~150-entry `admin` namespace** to `src/lib/i18n/dictionary.ts` (`nav`/`login`/`capture`/`review`, the last one covering every accordion/field/button on the Review screen — Header/Entrées/Featured/Soup/Combos/Veggie plate/Desserts/Sides, `PublishBar`, all the Spanish-translation UI added in step 3). One naming trap worth remembering: the Spanish-field placeholder strings (e.g. "the field for the dish name's Spanish translation") must be phrased *in the admin UI's current language*, not hardcoded to Spanish — the EN dictionary entry is `"Dish name (Spanish)"` (English words, telling an English-reading admin what the field is for), the ES entry is `"Nombre del platillo (en español)"` (Spanish words, same meaning, for a Spanish-reading admin) — first draft wrote raw Spanish phrases into the *English* dictionary object by copy-paste habit from the placeholder text, caught before shipping by re-reading the `en` block for stray non-English words. Also caught: `Accordion`'s section title ("Header") and its first field's label ("Title") both traced to variables meaning literally "title" in English, tempting a single shared dictionary key — they need separate keys (`header.sectionTitle` vs `header.title`) since one is a section heading and the other is a specific input's label, and collapsing them would either mislabel the accordion or the field. Verified live end-to-end: nav/capture/review all render correctly in both languages on a real draft (an already-existing 10-entrée/2-combo/27-side special), including accordion summaries pluralizing correctly ("10 artículos" vs "1 artículo") and field placeholders switching with the toggle; `tsc`/`eslint` clean. Not independently verified live: the login screen itself, since doing so would have required signing out of the only authenticated session available with no stored credentials to sign back in — verified via code review and type-check instead, following the identical pattern already proven working on every other screen. Left as a known gap, deliberately: error strings from *inside* an API response (a `ProviderError` message, a raw Supabase error) stay English — only the client-side fallback shown when a request fails outright is localized.
- **Extraction fix: "category header + shared-price option list" (2026-07-15):** a real board (Kountry Kitchen's) had "SLICE OF CAKE" as a header over five flavors (Coconut, Chocolate, Cheese Cake, Carrot, Seasonal Pies) all at one price ($4.95) — the vision model collapsed this into a single dessert item named "Slice of Cake," silently dropping all five flavors. `SPECIAL_MENU_EXTRACTION_PROMPT` (`src/lib/menu/special-menu-schema.ts`) now explicitly names this pattern and instructs extracting each option as its own item at the shared price. Verified the fix on a fresh extraction of the same photo (via `POST /api/menus/parse-special` with no `menu_id`, which creates a new draft from an existing asset without needing a new upload) — all 5 desserts came back correctly separated on the very first pass, no manual correction needed this time.
- **Renderer bug: hardcoded English structural labels (2026-07-15)** — see docs/08's "Bug found during end-to-end testing" note under Spanish Daily Specials. `renderSpecialMenuSvg()` drew several box titles/row labels as English string literals independent of the `DailySpecialMenu` data, so Spanish renders showed correctly-translated item names inside English-labeled boxes ("Soup of the Day", "Choose Your Sides"). Fixed with a third `locale` parameter and an internal `LABELS` table; every call site (2 in the Review screen, 2 in `renderAndStoreSpecial()`) now passes the locale matching the *data*, not the admin UI's display language. Caught only by testing a real publish and fetching+grepping the actual SVG text on both sides of the live site — invisible from the data layer alone, since `special_data_es` was already fully correct.
- **Standardized letterhead + board-wording fidelity (2026-07-15, owner's call):** three related Daily Special fixes after the owner reviewed a real generated menu against the handwritten sheet. (1) **Letterhead is standardized**: the rendered special's restaurant name / address / phone always come from the `restaurants` profile row, never from the photo — the extraction prompt now returns null for those three fields, `parseSpecialMenu` fills them from the profile, `renderAndStoreSpecial` re-enforces at render time (covers pre-rule drafts and any edits), and `review/page.tsx` overwrites on load so the client preview stays byte-identical to the persisted artifact. Both language renders carry the identical block (those fields are already excluded from translation). The Review screen's Restaurant name/Address/Phone inputs were **removed** (replaced by an explanatory note pointing at Settings) — editable fields that get silently overridden at render would be worse than no fields. Consistent with design rule #1: the values come from the profile row, nothing is hardcoded in code. **The date is the one exception (2026-07-15 follow-up, owner's call):** unlike name/address/phone, the board's own date IS read from the photo (into `dateText`) and rendered at the BOTTOM of the top-right letterhead block, below the standardized address+phone. The prompt was strengthened to reliably capture it (the date is usually handwritten near the title, e.g. "Happy Monday 7-13-26" → `dateLabel`="Happy Monday", `dateText`="7-13-26" — and it's explicitly exempted from the "ignore the letterhead" rule). `renderHeader` renders `dateText` (falling back to `dateLabel`), lightly emphasized. It's not translated (numeric, and excluded from the translation units), so it's identical on EN/ES. A board with no date (e.g. the "Beef tips" board, asset `7cb98f2d`) correctly shows no date line — `dateText` comes back null and the letterhead is just address+phone. Verified the feature on the dated board (asset `7f0c5806`, "7-13-26"): re-extraction read the date on the first pass, and the live EN+ES SVGs both show the letterhead as address → phone → "7-13-26". (2) **Printed subtitle capture**: the extraction prompt's old "preserve notes" rule wasn't strong enough — the printed "Entrée with 2 sides starts at $12.95. Additional side $2 more." line under the title was being dropped (it's printed, not handwritten, and the model treated it like letterhead). New explicit rule: capture printed instructional text near the title as `subtitle` exactly as written; only the letterhead is ignored. (3) **`dessertsLabel`**: new optional field on `DailySpecialMenu` (`.nullable().optional()` in Zod so pre-existing `special_data` still validates — a required field would have silently kicked old drafts into the legacy no-data Review path) holding the board's own dessert-section header exactly as written (e.g. "SLICE OF CAKE"); the renderer titles the desserts box with it when present, falling back to the localized generic "Desserts"/"Postres". It IS in the translation unit list (customer-facing copy), unlike the letterhead fields. Editable on the Review screen (new input at the top of the Desserts accordion, EN + ES). Verified end-to-end on the owner's real draft (`bcae3224`, found via `getLatestDraft`'s resume banner since it was extracted-but-never-rendered — not in the Library, which only lists rendered specials): re-extraction captured all three correctly on the first pass (including "Kountry fried steak" spelled with K, and the dessert flavors as bare "Coconut"/"Chocolate"/"Carrot" exactly as the board writes them under its header), translated ("PORCIÓN DE TARTA", subtitle translated with prices intact), rendered, published, and confirmed in the actual live SVGs on both EN and ES sides of `/` and `/menu`.
- **Daily Special format-robustness hardening (2026-07-15):** implemented from an audit of "how easily does the feature break if the board's format changes?" — see docs/08's "Format-robustness hardening" note for the full write-up. Five schema/renderer/translation/UI changes so the board is no longer constrained to a fixed shape: (1) **`additionalSections[]` catch-all** (`{title, note, items[]}`) — any section that doesn't fit the recognized categories (Breakfast, Appetizers, Kids Menu, Drinks…) is captured verbatim and rendered full-width below the sides, instead of the strict-schema forcing it into entrées/sides or dropping it. This is the headline fix — the single biggest fragility the audit found. (2) **Soup is now `soups[]` with generic `tiers: [{label, price}]`** instead of one hardcoded `{cupPrice, bowlPrice}` — the model reads the board's own size words (Cup/Bowl, Small/Large, or one unlabeled price); the LABELS table's `cup`/`bowl` entries and the renderer's Cup/Bowl literals are gone. (3) **`featured` is a list** too. (4) **Strikethrough prompt rule** (crossed-out items dropped, or flagged in `uncertainItems` if ambiguous). (5) `hasMeaningfulContent` now counts `veggiePlate`/`additionalSections` so a veggie-only or additional-only board can publish. **Backward compat is the load-bearing detail:** all stored `special_data`/`special_data_es` predates this shape, so `dailySpecialMenuSchema` is now `z.preprocess(migrateLegacySpecialData, baseSpecialMenuSchema)` — the migration wraps a legacy single `featured` object into `[obj]`, converts legacy `soup:{name,cupPrice,bowlPrice}` into `soups:[{name,tiers:[Cup,Bowl]}]`, and defaults missing `additionalSections`/`dessertsLabel`. It's idempotent (current-shape data passes through) and runs on every read of stored data (Review page load, render, translate). **Do NOT bypass `dailySpecialMenuSchema` and hand-read `special_data` — you'd get un-migrated legacy shapes.** Live-verified: a legacy draft (old cup/bowl soup) migrates into the new tiers and renders identically; a fresh extraction of a standard board produces clean array-shaped data (the soup came back with the board's own "cup"/"bowl" labels, proving the generic path); and a crafted board with two featured items, a single-price "Gumbo" soup, and Breakfast/Kids `additionalSections` all render correctly (each new section's heading, note, and items present in the SVG). The Review UI grew array add/remove for featured/soups (+ per-soup tier rows) and a new "Other sections" accordion, all with EN/ES fields and index-aligned EN/ES array helpers (same pattern as entrees/combos). The public path is unaffected — it renders the immutable pre-rendered snapshot image, never the `DailySpecialMenu` schema — so none of this touched the live site.
- **Translation glued-conjunction artifact (2026-07-15):** the live Spanish special showed "Pollo yÑoquis" (for "Chicken & Dumplings") — two words run together. Debugged by char-code inspection of the stored `special_data_es` before touching any code: the missing space was **in the translation model's output itself** (`y` immediately followed by `Ñ` U+00D1, no invisible characters), not a renderer/whitespace bug — the deterministic SVG renderer drew exactly what it was given. gpt-4o-mini occasionally glues a single-letter Spanish conjunction (y/e/o/u) onto a following capitalized word, seemingly around accented capitals. Fixed three ways: (1) `src/lib/i18n/normalize-translation.ts`'s `fixConjunctionSpacing()` — a deliberately narrow regex (`/\b([yeou])([A-ZÁÉÍÓÚÑÜ])/` → `"$1 $2"`) applied to every translated string in BOTH pipelines (`translate-special-service.ts` on each unit before `applyTranslations`; `main-menu/translate-service.ts` on each unit's name/description). Narrow on purpose: single-letter conjunction + capital is near-certainly the artifact, while a broader lowercase→uppercase splitter would mangle legitimate mixed-case names like "McRib" (unit-tested against exactly that: fixes "yÑoquis"/"oTazón"/"eHuevos", idempotent on correct text, leaves McRib/all-caps alone). (2) A "never run words together" rule added to both translation prompts. (3) The live data corrected, re-rendered, republished, and verified in the live ES SVG ("Pollo y Ñoquis" present, artifact gone). A full scan of the live special's ES strings found this was the only affected one.
- **Mailing-list capture: subscribers (2026-07-16):** new feature, not a fix — a homepage signup form (email and/or phone) plus an owner-only `/admin/subscribers` screen to manage the list. New `subscribers` table (migration `20260706000027`, docs/03) — deliberately its own table, no relation to `restaurant_members`/auth, since subscribers have no account. `POST /api/public/{slug}/subscribe` follows the established public-write pattern: service-role client (no session, docs/04), a honeypot field the route silently no-ops on rather than rejecting with an error (doesn't tip off anything sophisticated enough to retry), and the migration's two partial unique indexes (`(restaurant_id, lower(email))`, `(restaurant_id, phone)`, each `WHERE ... IS NOT NULL`) de-dupe resubmissions — caught as Postgres `23505` and turned into a friendly "already on the list" response, not an error. `/admin/subscribers` (`src/app/admin/subscribers/`) is this project's **first checkbox-multi-select + bulk-delete UI** and **first CSV export** — no prior pattern existed for either (confirmed by search before building), so both were designed from scratch rather than copied: bulk delete is `DELETE /api/subscribers` with a `{ids: string[]}` body (not the `/{id}` per-row pattern every other admin list uses, since the actual UI action here is "select several, delete together"); export is a plain `<a href="/api/subscribers/export">` letting the browser handle the `Content-Disposition: attachment` download natively, no client-side Blob/`createObjectURL` needed. Owner-only (not in the employee middleware allowlist, so already blocked there with no code changes needed) and English-only content per the established admin-translation scope (docs/04's step 4 note) — only its nav label is translated, matching Site Photos/History/Settings/Library. The public form **is** bilingual (dictionary's new `subscribe` namespace, both `en`/`es`) since it's customer-facing. **Verified live end-to-end once the migration was applied (2026-07-16):** build/`tsc`/`eslint` clean, plus every path driven through the real UI or its real API on the dev project — public form submits successfully (EN *and* ES, both success messages confirmed); resubmitting the same email returns `{ok:true, alreadySubscribed:true}` (the `23505` → friendly-response path) rather than an error; the honeypot returns a fake 200 **and writes no row** (confirmed by reading the table back — not just trusting the response); an unknown slug 404s and writes nothing; empty and malformed bodies both 400; phone-only and email-only rows both insert. Admin side: table renders both rows, nav link present, Delete disabled with nothing selected, select-all checks every box and the button label live-counts ("Delete selected (2)"), manual add creates a row correctly badged `source: "Manual"` and auto-closes/refreshes, a duplicate manual add 409s with the friendly message, selective bulk-delete removes *only* the checked row, multi-row delete clears the rest, and the empty state renders. CSV export returns `text/csv; charset=utf-8` + `Content-Disposition: attachment; filename="subscribers-YYYY-MM-DD.csv"` with a header row and — correctly — only the rows that actually have an email (the phone-only subscriber is excluded, which is the intended "export **emails**" behavior). Responsive: stacks full-width at 375px, side-by-side row at 1280px, no page overflow at either; honeypot confirmed `display:none` / 0×0 / `tabIndex -1` / `aria-hidden`. All test rows were deleted afterward — the table is back to empty. No console errors anywhere.
  **Privacy-policy fallout — found while doing a state/docs sweep, NOT reported (2026-07-16):** adding a mailing list silently falsified the live public `/privacy` page, which is a legal document. Worst of it: the policy said outright **"We don't add you to a mailing list"**, and its own "Changes to this policy" section promised *"if we add ... an email list — we'll update this page to describe it **before that feature goes live**."* Shipping Subscribers broke that promise the moment it went live. Also found stale on the same page, and **predating this session's work** (i.e. two separate earlier changes shipped without updating the policy — the pattern, not the one bug, is the real lesson): (a) "sign in with their email address only — **there's no password to store**", false since the 2026-07-14 email+password auth switch; (b) "This site **does not use cookies**... of any kind", false since the `site_locale` language toggle — docs/09 had even *noted* that cookie in its own text on 2026-07-15 while nobody updated the customer-facing page it supports; (c) the Daily Specials/OpenAI description still said "AI **image-generation** tool", stale since the extract-and-render refactor, and never mentioned the `translate_menu` flow at all. Fixed all of it: rewrote the AI section to describe reading-and-redrawing plus translation, corrected the auth and cookie claims, added an "Our email list" section, narrowed "Contacting us" to "we won't add you to our list *just because you contacted us*", and dropped the now-broken before-it-goes-live promise. **Also aligned the signup form's own copy with reality** — it promised "no spam, unsubscribe any time" / "sin spam, cancela cuando quieras" in both languages while **no unsubscribe mechanism exists** (the only way off is the owner deleting the row); reworded both to "just tell us any time to take you off the list", which is what's actually true today. docs/09 now carries a "Subscribers, reconsidered" section recording the real remaining gaps (no unsubscribe/opt-out token, no retention policy, no rate limiting, no consent record) and flags that a tokenized unsubscribe should exist **before the list is ever mailed** — it's also the practical way to meet CAN-SPAM's opt-out duty later. **Standing rule this implies: any change to what the app collects, stores, or sends is also a `/privacy` + docs/09 change — that page is product surface, not documentation, and it makes specific factual claims that go stale silently.**
  **Tokenized unsubscribe — closes the honesty gap above (2026-07-16, owner's call: "build the tokenized unsubscribe link before we mail anyone"):** migration `20260706000028` adds `unsubscribe_token` (32 random bytes hex, unique) + `unsubscribed_at` to `subscribers`; new public `/unsubscribe?token=` page (`(marketing)/unsubscribe/`, bilingual, `noindex`, `force-dynamic`) + `POST /api/public/unsubscribe`. The signup copy's "unsubscribe any time" / "cancela cuando quieras" was restored — it's now true. **The four decisions worth not re-litigating**, each of which a naive implementation gets wrong: (1) **suppress, don't delete** — `unsubscribed_at` is set and the row stays, because deleting loses the record of the opt-out and a later manual add/CSV re-import would silently re-subscribe them; (2) **the export is the enforcement point** — it filters `unsubscribed_at is null` and carries a per-row `unsubscribe_url` (origin from `new URL(request.url).origin`, **not** `NEXT_PUBLIC_SITE_URL`, which CLAUDE.md documents as unset on Netlify and would emit localhost links into real mail), since until sending exists the CSV *is* the mailing surface and a token that never reaches an inbox is just a column; (3) **POST confirms, GET never mutates** — corporate mail scanners (Outlook Safe Links etc.) GET every URL in a message, so a mutating GET would unsubscribe people who never clicked; verified live by fetching the link and confirming the subscriber was *still* in the export afterward; (4) **asymmetric re-add rules** — `createSubscriberByOwner` 409s on a suppressed contact (the owner can't revive an opt-out) while `createSubscriberPublic` clears `unsubscribed_at` (the person's own submission *is* the opt-in). That last branch is the subtle one: without it the unique index rejects their signup, they're told "already on the list," and they stay suppressed forever — subscribed in their mind, opted-out in ours. Also: `unsubscribe_token` is kept out of `listSubscribers`' explicit column allowlist (`SubscriberListItem`) so it never lands in the admin page's serialized HTML — only the export reads it; the token is stable across an unsubscribe/resubscribe cycle so already-sent links keep working; unknown tokens return a generic 404 (non-probeable); repeat clicks are idempotent. **Verified live end-to-end after the migration**, in both languages: GET-doesn't-unsubscribe, confirm→removed, excluded from export while the row remains with a timestamp, second visit says "already off the list", double-POST 200s, owner re-add 409s with the explanatory message, self re-signup restores them with token intact and no duplicate row, admin shows Subscribed/Unsubscribed, unknown/empty tokens 404/400, `noindex, nofollow` present, masked contact (`al•••@example.com`) rendered. Test rows deleted afterward. **Known and deliberate:** a phone-only subscriber has a token but no way to receive it (tokens only surface via the *email* export) — unreachable today since nothing sends SMS, but delivering it is part of the work if SMS is ever built (docs/09). **One real bug caught during verification, worth remembering:** the admin page's intro rendered "manually.Export emailsgives" — JSX drops a newline that sits directly against a tag, so the spaces around an inline `<strong>` on its own source line silently vanish. Use explicit `{" "}` rather than trusting source-line breaks for spacing around inline elements.
  **Florida compliance re-audit (2026-07-16, owner's call, after the signup + unsubscribe shipped):** re-audited from **source, not from these notes** — a subagent inventoried every input, API route, third-party call site, browser-storage write, and deletion path with file:line evidence. Findings: the low-exposure posture holds (one customer-facing input on the whole public site; **no customer PII reaches any third party** — verified by inspecting all five AI call sites *and* confirming `subscribers/service.ts` is imported by no provider path; **zero** analytics/tracker/error-reporting packages; no localStorage/sessionStorage/IndexedDB anywhere; all eight `console.*` calls log Supabase error objects and never PII; RLS on). **But it found two real defects, both fixed:** (1) **the Gemini adapter put the API key in the URL** (`?key=`) while OpenAI/xAI use an `Authorization` header — URLs land in access logs, which contradicted `crypto.ts`'s own rule *and* made `/privacy`'s "never … written to a log" claim false; switched to the `x-goog-api-key` header (latent, since no live task uses Gemini). (2) **the subscribe endpoint was a membership oracle** — it returned `alreadySubscribed`, so anyone could POST an address to an unauthenticated open-CORS endpoint and learn whether it was on the list; the tell was that the *unsubscribe* route already defends against exactly this, so two routes disagreed on one threat. Now returns an identical `{ok:true}` either way (the service still distinguishes internally to un-suppress returning subscribers — it just doesn't tell the caller); the dead `alreadySubscribed` dictionary strings and client branch were removed with it. Verified live: two signups of the same address return byte-identical responses, and re-subscribe still silently clears suppression. Also deleted an empty `src/app/api/dev/set-session/` directory (leftover scaffolding, no route file). **Stale claims corrected across the docs in the same pass** — most notably "**Supabase Vault**", which appeared as fact in CLAUDE.md, docs/01, docs/02, docs/03, and docs/08 and **was never true**: provider keys are AES-256-GCM app-side (`crypto.ts`), and docs/03 additionally still said `bytea` when the column is base64 `text`. docs/09's retention section also overstated things — it described an account-closure cleanup that doesn't exist; corrected to state plainly that **nothing is deleted automatically anywhere** (no TTL, no pruning; the `on delete cascade` chains never fire because nothing deletes a restaurant) and that deletion is manual via exactly two owner paths. **Left as an open recommendation, deliberately not silently changed:** `subscribers`' RLS is `for all` to any member, so an *employee* has DB-level read/write on the mailing list and only the middleware stops them — consistent with the documented "role boundary lives in middleware" pattern, but that's the table where the pattern has least margin; an owner-only policy would be strictly tightening and cost nothing functionally, but it deviates from a settled decision, so it's a call to make rather than an edit to sneak in (docs/09). **[RESOLVED 2026-07-16 — the owner made that call; see the "subscribers hardening pass" note below.]**
  **Signup form field widths (2026-07-16 follow-up, reported live: "the phone number field... stays way smaller than it needs to"):** the phone input's own `sm:max-w-[220px]` looked like the culprit but **was never the binding constraint** — measuring live showed the *form container*'s flat `max-w-lg` (512px) was, since that 512px had to fit two inputs *plus* the submit button: each field collapsed to ~183px (EN) / ~105px of inner text space (ES, whose "Quiero recibir novedades" button is ~215px vs English's ~122px). At those sizes the **placeholder was actually being truncated** — EN's "Phone number (optional)" needed 156px but had 151px; ES's "Número de teléfono (opcional)" needed 192px but had 105px, *and* ES's email placeholder truncated too. Fixed by widening the container (`max-w-lg md:max-w-3xl`), dropping the phone's arbitrary cap (both inputs are plain `w-full` now, so flex splits the row evenly), and moving the row breakpoint `sm:flex-row` → `md:flex-row` (at 640px the container is still `max-w-lg` and three-across genuinely can't fit ES without truncating, so 640–767px stacks full-width instead). Landed on `3xl` not `2xl` specifically because ES still truncated by 7px at 2xl — a reminder that this project's EN-only measurements are not sufficient for any layout carrying translated text (same lesson as the SiteNav 460px threshold note above). Verified live in **both** languages at 375/767/768/1024/1280/1920: EN phone 183px → 311px, ES 183px → 265px, zero truncation anywhere, clean stack↔row transition at the 767→768 boundary, no page overflow, and the form stays centered at ultrawide. (A `3xl` = 768px form is wider than the section's `max-w-md` body copy above it, which is intentional and looks correct — a form row and a paragraph don't need matching measures; on a real 768px tablet the section's own `px-5` clamps it to 713px anyway, so `3xl` costs nothing there.)
- **Subscribers hardening pass — four open items closed (2026-07-16, owner's calls on the two that needed decisions):**
  (1) **`subscribers` RLS is owner-only now** (migration `20260706000029`) — a **deliberate, recorded exception** to the "role boundary lives in the middleware, not in RLS" pattern: customer PII is the one place a single middleware mistake would otherwise be the only barrier between an employee account and every customer's contact details. Strictly tightening (employees were already route-blocked; every session-scoped use of the table is behind `/api/subscribers`; the public paths use service-role, which bypasses RLS). If another table ever seems to want the same, decide consciously against this precedent — don't copy the exception *or* the pattern blindly. **Verified live with real user JWTs (anon key + signed-in session, not service-role), including the control that makes the result meaningful:** a temp *employee* who is a genuine linked member — `auth.uid()` resolves, and they still read `menus` (5 rows) and `main_menu_sections` (5 rows) through the normal any-member policies — sees **0 rows** from `subscribers` and gets an explicit `new row violates row-level security policy` on insert, while a temp *owner* reads and writes normally. Without that control the employee's empty result would have been indistinguishable from "not a member at all," i.e. a false pass. Note for reads, RLS denial **is** an empty result set, not an error — don't expect a thrown error when testing SELECT.
  (2) **Rate limiting on the two public POSTs** (migration `20260706000030` + `src/lib/rate-limit.ts`): Postgres-backed fixed windows (subscribe 10/hr, unsubscribe 30/hr per client), because Netlify serverless makes in-memory counters useless (cold starts, multiple instances) and an external store would contradict docs/01's low-maintenance stance. The counter key is an **HMAC-SHA256 of the client IP** (domain-separated reuse of `PROVIDER_KEY_ENCRYPTION_SECRET`; `x-nf-client-connection-ip` on Netlify, XFF fallback in dev) — **the raw IP is never stored** (docs/09; disclosed on `/privacy`), and rows self-sweep after a day inside `bump_rate_limit()`. **Fails open on any error** — a DB hiccup, or this code deploying before its migration runs, degrades to "no rate limiting," never a blocked signup or opt-out (that ordering-safety is deliberate, per the `.select()`-before-migration lesson above; **observed for real** — the routes were driven live pre-migration and the server logged `PGRST202 could not find the function` while still returning `{ok:true}`). 429s get their own localized `rateLimited` dictionary strings in both clients (EN words in the `en` block — the step-4 naming trap). Residual, accepted: distributed floods from many IPs still pass per-IP limits; worst case stays a bulk-deletable spammed list. **Verified live post-migration:** requests 1–10 pass and 11–12 return 429 (exact cutoff, driven through the real endpoint); the EN and ES forms each render their *client dictionary* string ("…from your connection" / "Demasiados intentos desde tu conexión…"), proving the status→message mapping fires rather than echoing the server body; the stored key is `subscribe:<32-hex>` with **no raw IP anywhere** (grepped the live table for IP-shaped content — the `/privacy` claim is accurate); and, critically, **an opt-out still succeeded (200) while subscribe was fully 429ing for the same IP** — the scopes are independent counters, so a spam flood cannot block someone leaving the list. **`rate_limit_counters` deliberately has no `restaurant_id`** — a counter is keyed by client, not tenant, so design rule #2's multi-tenant seam doesn't apply (infrastructure, not restaurant data; recorded in docs/03, not silently diverged).
  (3) **Retention is a written policy, not code (owner's explicit choice over auto-pruning):** "nothing is deleted automatically; the owner deletes on request; anyone can ask for full erasure" — now stated on `/privacy` and adopted in docs/09. Auto-pruning suppressed rows was rejected because it reintroduces the exact silently-re-subscribed-after-opt-out failure suppression exists to prevent, and pruning "stale" subscribed rows is meaningless while nothing has ever been mailed (no engagement signal). Revisit when sending/SMS ships or the list gets large.
  (4) **Phone CSV export** (`GET /api/subscribers/export/phones`, "Export phones" in `/admin/subscribers`): every still-subscribed row with a phone + its `unsubscribe_url`, closing the "phone-only subscriber has a token but no way to ever receive it" gap. Email export untouched; both exclude suppressed rows. Explicitly **not** an SMS surface — mini-TCPA (§501.059) written consent isn't captured at signup, and the button's own title copy + `/admin/subscribers` intro say so. A public "enter your email/phone to unsubscribe" form was considered and **rejected**: it would let anyone unsubscribe anyone and re-open the membership-enumeration oracle the subscribe endpoint deliberately closed (its identical-`{ok:true}` response is intact — re-verified this pass). Still open, recorded in docs/09: no double opt-in/consent record (design-before-build if SMS ever happens).
  **Real pre-existing bug caught by this pass's live verification (fixed same day):** `findConflicting()` (`src/lib/subscribers/service.ts`) built a raw PostgREST `.or()` filter string with the contact value embedded — but `(`, `)`, `,` and spaces are *structural* in `.or()` syntax, so any real-world phone (`(239) 555-0142`) silently matched nothing, no error. Consequences: a suppressed subscriber's own re-signup returned `{ok:true}` **without clearing their suppression** (subscribed in their mind, opted-out in ours — the exact failure the suppression design warns about), and the owner's re-add of a suppressed contact got the generic "already on the list" 409 instead of the unsubscribed explanation. Invisible in earlier verification because that used plain emails, which have none of those characters. Fixed with two parameterized `.eq()` lookups (supabase-js URL-encodes `.eq()` values; it does NOT escape `.or()` strings — **never interpolate user data into a `.or()`/`.filter()` string anywhere in this codebase**), and verified live both ways on the parenthesized phone: suppressed re-add → correct 409 message; own re-signup → `unsubscribed_at` actually cleared.
- **Jobs + Email/Fax List pages, and the nav's inline threshold moved lg → xl (2026-07-16, owner's request):** two new public pages (`(marketing)/jobs/`, `(marketing)/email-fax-list/`) added to `navLinks()` in `(marketing)/layout.tsx` — **that one array drives both the header and the footer nav**, so a page added there appears in both; being in the `(marketing)` route group is what gives them the shared header/footer/template transition for free. Both are deliberate placeholders (centered `<h1>` + the shared `common.pageComingSoon` line) until the owner populates them; kept minimal rather than literally blank so they read as unwritten rather than broken. Nav labels are translated like all site chrome (`nav.jobs`/`nav.emailFaxList`; ES "Empleos"/"Lista de correo/fax") — EN words in the `en` block, per the step-4 naming trap. **Each page carries a comment flagging that populating it is probably a `/privacy` + docs/09 change** (a jobs form and an email/fax signup both collect personal data), and Email/Fax List's points at the existing `subscribers`/`SubscribeForm` path as the thing to reuse rather than duplicate — plus the detail that *fax* is a contact type the schema and Zod have no column or rule for.
  **Jobs page built out — screenshot-of-the-form-as-link (2026-07-16, owner's explicit call after being shown the trade-offs):** `/jobs` is now real: prospect info in `content.ts`'s `jobs` block (intro / "What to expect" / equal-opportunity), bilingual, plus a **cropped screenshot of the owner's Microsoft Forms employee application (`https://forms.cloud.microsoft/r/Xq2ubBhgLp`) wrapped in an `<a target="_blank" rel="noopener noreferrer">` to the real form**. It links out — **no iframe/third-party embed** — so docs/09's zero-tracker, zero-third-party-script posture is untouched, and the page collects nothing itself (no `/privacy` change needed; that holds only while it links out rather than hosting a form).
  **Recapture recipe (the image is a point-in-time copy of someone else's page and goes stale silently — nothing detects it):** `chrome.exe --headless` (**classic, NOT `--headless=new`** — the new mode exits without writing a file here; Chrome 150) `--disable-gpu --no-sandbox --hide-scrollbars --force-device-scale-factor=2 --window-size=1200,1600 --virtual-time-budget=25000 --screenshot=raw.png <url>`, then `sharp` `.extract({left:350,top:100,width:1700,height:2156})` — that box is the form card's own measured bbox (CSS `175,50 850×1222` at viewport 1200×1600, ×2 for the 2x capture), with the height stopping at CSS y=1128 where the white form ends so Microsoft's logo bar + legal disclaimer are cropped off along with the background photo. Emit **two** widths (850/1700) as webp q90 into `public/jobs/` behind a plain `<img srcset>` — deliberately **not** `next/image` (no dependency on the Netlify image runtime, given this project's documented friction there), so a phone still gets 131 KB instead of 296 KB. Headless layout was verified to match the live DOM measurement exactly before cropping.
  **Known and accepted, since the owner chose this over an Apply button:** the image is a picture of dense legal text, so it is **unreadable on a phone** — measured live: 333px wide at a 375px viewport = 39% of native = **~6px effective body text** (it's ~12.5px at 768px and above, i.e. fine on tablet/desktop). It's therefore decorative on mobile, and the **text link beneath it is not decoration — it is the only working path on a phone and for screen readers** (the image's alt gives the link's *destination*, per image-in-link practice, not the form's contents); don't remove it. The copy also states outright that it's "a preview… tap it to open the real thing", because the screenshot shows an **"I Agree" checkbox and a "Next" button that are only a picture** — without that line a visitor could reasonably believe clicking it agreed to something. Page 1 is a legal attestation, so pages 2–4 were never seen (reaching them means ticking that box as an applicant — not done, not without the owner's say-so).
  **Live form defects found and reported, NOT fixed (owner chose "leave the form as-is"; they're in Microsoft Forms, not this repo):** the application's description contains pasted **AI-assistant preamble** — "Aquí tienes una traducción profesional y natural para tu formulario:" and "Aquí tienes una versión corregida y pulida de la traducción…" — i.e. the first thing a Spanish-speaking applicant reads (a large share in Immokalee); the Spanish legal text has **sentences glued together** with no space after the periods (`deliberadamente.Acepto`, `empleo.Autorizo`) — the same artifact class as the `Pollo yÑoquis` note above; and English has a stray "…fired from Kountry Kitchen**I**." **All three are baked into the committed screenshot** — re-capture after any form fix. Content rule for this page: `content.ts`'s `jobs` is grounded only in what the form itself states (4 pages, the attestation's contents, EEO + accommodation) — **no claims about open roles, pay, hours, or hiring process**, none of which are known.
  **Email/Fax List built out as a NATIVE form + new table (2026-07-16, owner's pivot mid-task):** the original ask was "same as Jobs" (screenshot of the owner's MS Form `Fax and Email Preference For Daily Special`, linked out) — the owner explicitly changed course to "replicate the form in our own site and log each entry in a new table." So `/email-fax-list` is now a bilingual native form (business/first name; fax/email/both; contact fields shown conditionally by method; Mon–Sun day picker reusing `common.days`; notes) posting to `POST /api/public/{slug}/email-fax-list` → **`email_fax_requests`** (migration `20260706000031`). Decisions that shouldn't be re-litigated: (1) **it's a LOG — one row per submission, no unique indexes, no dedupe** (owner's words: "log each entry"); duplicates are reconciled by the owner at `/admin/email-fax-list` (list + multi-select bulk delete, subscribers-style; new admin nav entry, owner-only). (2) **Deliberately not merged into `subscribers`** — different consent (the daily menu itself vs. news), fax is a new contact type, and the owner asked for a new table. (3) **Owner-only RLS** — second conscious use of the subscribers exception; its scope is now "customer-PII tables," still not a default. (4) **Validation is stricter than the MS original** (which required nothing): name + method required, and each named channel requires its contact — enforced in Zod *and* as table CHECKs; the service also nulls out the channel the chosen method doesn't use, so a fax number typed before switching to email-only is never stored. (5) **Honeypot field is `website`, not `company`** — this form has a real business-name field, and a hidden "company" input next to it invites autofill false positives. (6) Rate limited via the existing `bump_rate_limit()` with a new `email_fax` scope (10/hr) — scopes are just strings, no new migration needed for that. (7) **No unsubscribe token, deliberately** — the app sends nothing; removal is "contact us," and docs/09 records the flip side: rows double as the **Junk Fax Act consent record** (47 U.S.C. §227 — fax ads need prior express permission, and this form IS it), so the admin intro warns not to delete someone still being faxed. `/privacy` gained its own section ("The daily special, by fax or email") and the rate-limit disclosure was widened from "the sign-up and unsubscribe forms" to all forms — the placeholder page's own "building this = a /privacy change" comment came true and was honored in the same pass.
  **(8) Netlify Forms notification (owner's follow-up ask, same day):** after the Supabase insert succeeds, the route forwards the submission server-side to Netlify Forms (`src/lib/email-fax/netlify-notify.ts` → url-encoded POST to `{origin}/__forms.html`) so the owner gets Netlify's email notification. The parts that aren't obvious: (a) **`public/__forms.html` is load-bearing** — Netlify's build scanner only registers forms found in static HTML, and on the Next.js runtime every page is a function it can't see; that hidden static definition is the documented workaround, and its field names must stay in sync with what the route forwards. (b) **Insert first, notify second, never the reverse** — the forward is best-effort (5s timeout, all failures swallowed to `console.error`), so DB and notifications can only diverge in the safe direction: a saved entry with a missed email, never an email for an unsaved/rejected submission (honeypot/rate-limit/Zod all run before it). In local dev the POST just 405s and logs — expected, not a bug. (c) **Owner-side setup, one time, in the Netlify UI:** enable form detection (Site configuration → Forms), then after the next deploy add an email notification for the `email-fax-list` form (Forms → Form notifications). (d) **This made Netlify a *store* of customer PII, not just the host** — the one flow whose PII leaves Supabase; docs/09's "no customer PII to any third party" claim was re-scoped to "no AI provider" with the Netlify exception recorded, `/privacy`'s Netlify bullet + fax/email section disclose it, erasure-on-request now includes deleting the Netlify Forms copy, and Akismet can silently divert a legit notification to Netlify's spam folder (the admin list is the source of truth, not the inbox). (e) **End-to-end delivery is only verifiable on a real deploy** — the interception happens at Netlify's CDN; locally only the fail-open path can be (and was) observed. Free tier is ~100 submissions/month — plenty here, but it exists.
  **Admin nav crowding, third occurrence — caught by measuring before shipping this time:** the new "Fax/Email List" link made the admin nav 8 items + Sign out, and at its then-`lg` threshold the Spanish labels overflowed by a measured 189px at 1024px (page-level horizontal scroll). Three coordinated fixes in `admin-nav.tsx`, each verified live: (1) inline threshold `lg` → `xl` (every `lg:` moved together, grepped for stragglers); (2) **the container cap was the real constraint, not the breakpoint** — the bar's `max-w-5xl` (1024px) meant the ~1230px ES row overflowed at *every* viewport width, so raising the breakpoint alone fixed nothing (measured: still 174px over at 1280); fixed with `xl:max-w-none`, chosen over a fixed `xl:max-w-7xl` because a 1280px cap froze ES's 12px boundary slack as the *permanent* maximum at all widths, whereas uncapped it grows (measured 171px slack at 1440); (3) the Sign-out button needed `whitespace-nowrap` like the links — under flex pressure at the boundary "Cerrar sesión" wrapped to two lines inside its own button and stretched the whole header to 77px (now a single-row 57px). Verified EN+ES at 375/768/1024/1279/1280/1440: zero header/page overflow everywhere, clean drawer↔inline flip at 1279/1280, drawer holds all 9 items right-aligned (dropdown rule) incl. the new link. **ES fits the 1280 boundary with 12px of slack — re-measure both locales at 1280 before adding or renaming any admin nav item.**
  **The load-bearing part: going 6 → 8 links (9 off-homepage, where the Home link also renders) broke the header at `lg`, and English alone would have shipped it.** At 1024px in Spanish the bar overflowed itself by **81px** and pushed the whole page into horizontal scroll; in English it fit with **exactly 0px to spare** — "passing" by a margin one label change would erase. This is the third time this bar's crowding has bitten (sm→lg at 7 links; admin-nav's own md→lg), and the same fix applied: raise the inline threshold, `lg` (1024px) → `xl` (1280px), moving **every** `lg:` in `site-nav.tsx` together (grid/logo/inline nav/right cluster/top bar/drawer) so the tiers can't disagree — grepped for stragglers after. Between 1024–1279px the hamburger now serves, which is strictly better than a broken bar. The <460px CTA tier is untouched. Verified live in **both** locales on `/about` (worst case, 9 links) at 375/459/461/768/1023/1279/1280/1920: zero header and page overflow at every width, clean flip at the 1279→1280 boundary, logo never wraps (28px), CTA never wraps (36px), exactly one CTA + one toggle visible at every tier, both new links present and `text-right` in the 256px/24px-gap drawer, and the drawer still floats (hero pixel-identical open vs. closed) and paints over content. Footer heights unchanged from their documented baselines (145px @1920, 197px @768 EN, 203px @768 ES) — its nav wraps, and its height is driven by the brand/legal columns, not the link row. **The component comment's own standing instruction — "re-verify BOTH locales if the threshold, the CTA copy, or the link set ever changes" — is what caught this; it's been updated with the new boundary and kept.**
